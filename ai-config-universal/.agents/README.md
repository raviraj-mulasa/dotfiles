# AI Engineering Configuration

This project uses `ai-config-universal` for AI-assisted development.

Skills and agent role definitions are in `.agents/skills/` and `.agents/agents/`.

AGY (Antigravity) auto-discovers this `.agents/` folder and loads skills and rules from it.
